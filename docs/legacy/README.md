# Legacy artifacts

This directory is reserved for historical output from the original single-file
implementation.

`Comparison_of_3_DRLs.png`, when present, was produced before the version 2.0
experiment contract. It has no associated resolved configuration, dataset
fingerprints, checkpoints, runtime metadata, seed schedule, or machine-readable
metrics. Its action-agreement and hand-written rollout views are not comparable with
reports generated by the current pipeline.

The image is retained only to document project evolution. It must not be cited as a
current benchmark or used to support an algorithm-ranking claim. Generate new
evidence with the commands in the
[reproducibility protocol](../REPRODUCIBILITY.md).
